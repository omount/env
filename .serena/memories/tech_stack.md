# Tech stack

- Shell: bash-oriented `.sh` notes/scripts; authoring often on Windows PowerShell.
- Docker / Docker Compose V2 (`docker compose`, no Compose `version:` field).
- GitLab CE Omnibus image pin: `gitlab/gitlab-ce:18.9.2-ce.0`.
- Bun: official installers (PowerShell on Windows; curl|bash on macOS/Linux).
- No app language runtime, package.json, Makefile, or CI in repo today.
- Encoding: UTF-8.
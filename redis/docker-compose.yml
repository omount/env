services:
  redis:
    image: redis:7.4
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    command:
      - redis-server
      - --requirepass
      - "123456"
      - --appendonly
      - "yes"

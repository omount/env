services:
  pgsql:
    image: postgres:16
    container_name: pgsql
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: "123456"
      POSTGRES_DB: root
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
